# Currency Service

The Currency service provides functionality for converting prices between different currencies.

## Proto Files

Proto files are placed inside `src/currencyservice/proto` after running `genproto.sh`:

```
./genproto.sh
```

Original proto files should be placed at `../../protos` before executing the script.

## Build

From `src/currencyservice`, run:

```
docker build ./
```
