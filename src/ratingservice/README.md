# Rating Service

The Rating service provides functionality for managing and retrieving product ratings and reviews.

The initial rating state is defined in `ratings.json` and is not persistently stored in a database.

## Build

From `src/ratingservice`, run:

```
docker build ./
```
