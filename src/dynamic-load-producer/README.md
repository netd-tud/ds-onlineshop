# Dynamic Load Producer

The Dynamic Load Producer service provides functionality for generating dynamic load for the monitoring demo.

## Build

From `src/dynamic-load-producer`, run:

```
docker build ./
```
