# Load Generator

The Load Generator service provides functionality for generating load on the application to generate analytics data.

## Build

From `src/loadgenerator`, run:

```
docker build ./
```
